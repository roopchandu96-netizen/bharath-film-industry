import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bharatfilmindustry.app',
  appName: 'Bharat Film Industry',
  webDir: 'dist'
};

export default config;
